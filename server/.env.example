# Server settings
PORT=5001

# PostgreSQL connection settings
CRUD_HOST=postgres              # Match the service name in docker-compose
CRUD_PORT=5432
CRUD_NAME=your_database_name
CRUD_USER=your_postgres_user
CRUD_PASSWORD=your_postgres_password

# JWT or session secrets (if needed)
JWT_SECRET=your_jwt_secret_here

# Optional: CORS origin
CLIENT_URL=http://localhost:3000
